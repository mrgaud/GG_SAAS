
<div class='jumbotron'>
    <h1 class='text-center'><i class="fa fa-users"></i> Gamer Gala</h1>
    <h3 class='text-center'>Bringing the gaming community closer.</h3>
</div>
<% if current_user %>
<% else %>
    <div class='row'>
        <div class='col-md-6'>
            <div class='well'>
                <h2 class='text-center'>initiate</h2>
                <h4>A basic account for people only looking to attend.</h4>
                <%= link_to "Sign Up", new_user_registration_path(plan: @basic_plan.id), class: 'btn btn-lg btn-primary btn-block', method: :get %>
            </div>
        </div>
        <div class='col-md-6'>
            <div class='well well-background'>
                <h2 class='text-center'>Master</h2>
                <h4>A premium account for those looking to host parties.</h4>
                <%= link_to "Sign Up", new_user_registration_path(plan: @pro_plan.id), class: 'btn btn-lg btn-success btn-block', method: :get %>
            </div>
        </div>
    </div>
<% end %>
